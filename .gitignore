# Generated output — Netlify rebuilds this from source on every push.
_site/
_site_test*/

# Dependencies
node_modules/

# Environment files (build-time/server-side only; never commit secrets)
.env
.env.*

# OS noise
.DS_Store
